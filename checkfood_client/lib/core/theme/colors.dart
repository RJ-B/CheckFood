import 'package:flutter/material.dart';

/// Centrální paleta barev aplikace CheckFood.
///
/// Odvozena z grafického manuálu (splash screen + Material 3).
/// Používejte tyto konstanty místo přímých hex hodnot.
class AppColors {
  static const primary = Color(0xFF10B981);
  static const primaryLight = Color(0xFFD1FAE5);
  static const primaryDark = Color(0xFF059669);

  static const brandDark = Color(0xFF1A3C40);
  static const brandGradientStart = Color(0xFF0F2027);
  static const brandGradientMid = Color(0xFF203A43);
  static const brandGradientEnd = Color(0xFF2C5364);

  static const background = Color(0xFFFAFAFA);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceElevated = Color(0xFFFFFFFF);

  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF6B7280);
  static const textMuted = Color(0xFF9CA3AF);

  static const border = Color(0xFFE5E7EB);
  static const borderLight = Color(0xFFF3F4F6);

  static const success = Color(0xFF059669);
  static const successLight = Color(0xFFD1FAE5);
  static const warning = Color(0xFFD97706);
  static const warningLight = Color(0xFFFEF3C7);
  static const error = Color(0xFFDC2626);
  static const errorLight = Color(0xFFFEE2E2);
  static const info = Color(0xFF2563EB);
  static const infoLight = Color(0xFFDBEAFE);

  static const overlay = Color(0x80000000);
}
